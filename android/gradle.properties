android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# The site the app opens. Overridden per build with -PtwaHost=<host>, so
# building against a staging host never needs a commit.
twaHost=chronos.tevproject.com
